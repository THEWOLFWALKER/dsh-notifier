# Project state

Snapshot date: 2026-09-25. Current release line **v0.11.0** (issue/PR zeroing + DSH host alignment + ecosystem public surface; **1816 tests, all pass**). The 0.11.0 release closes the 2026-09 issue/PR backlog and aligns the plugin with DSH `dsh-v0.1.7-rc.1`: W0 clearouts (#26 QQ keyboard `permission` `{ type: 2 }` + button label ≤10 code points; #31 bounded inbound transport timeouts — QQ/DingTalk `AbortController`, Feishu via an isolated bounded SDK `httpInstance`; #32 turn/end body moved to a bounded assistant-text cache and `session.events`/`snapshotEvents` fully removed; #33 QQ outbound default markdown + code-point segmentation; #36 QQ `attachments` image/file durable admission), plus the Feishu P2P `ou_/oc_` source mismatch fix (PR #20 residue) and the wps-bot `http:` plaintext-credential tightening (PR #34 review). Host alignment: P0-A inbound images become durable image blocks with `source.kind='dsh-notifier'` (UserMessage V4), P0-B `/stop` · turn/cancel send a structured `AgentCancelCause {kind:'user'}`, P1 Qmsg 3.0 v3 endpoint/target migration + ServerChan SC3 numeric-shard endpoint + `@deepseek-ai/dsh-session` optional peer compatibility matrix, P2 host-events current-ctx-first with sticky root fallback and the native-question seam recalibrated to the `user-questions/request` waterfall. Ecosystem public surface: W1 `dsh-notifier/testing` fake, W2 `dsh-notifier/types`, W3 `examples/consumer-demo`, W4 `PLUGINS.en.md`, W5 mobile `/sessions`, W6 mobile `/log` (off by default, owner-only, redacted, bounded). Planned W7 (admin policy templates) / W9 (`test/reliability-*` contract pack) / W8 (`docs/reliability.md`) / W10 (admin channel health panel) are **deferred to the next release** (spec retained in [ROADMAP.md](../ROADMAP.md)). Previous line: v0.10.2 doc cleanup + listing sync (1616 tests); v0.10.1 notification-lang-setting + PR #22 (bilingual `lang` i18n + Telegram ask_user aux buttons, 1616 tests); v0.10.0 mobile task loop (1605); v0.9.7 pr22-issue23-fix (1548); v0.9.6 zero-config onboarding (1544); v0.9.5 closed at 1531. The 80-item 2026-08 review list is reconciled: 70 fixed, G-19 evidence-logged, 9 registered non-fix (G-35/36/37; S-01/03/08/09/10/15). Test-count baselines in release guards are updated at train close, not per-commit.

- Current development validation: `npm test` = **1816** total, **1816 pass** (2026-09-25, v0.11.0 release baseline); v0.10.2 close baseline was **1616** (2026-09-13).
- Historical npm `0.8.6` artifact carried contract count **909**; `0.9.0` used **1352**; v0.9.5 closed at **1531**; v0.9.6 closed at **1544**; v0.9.7 closed at **1548**; v0.10.0 closed at **1605**; v0.10.1/v0.10.2 were **1616**; the current v0.11.0 line is **1816**.
- Single-repo two-branch model: `THEWOLFWALKER/dsh-notifier` hosts `dev` (development) and `main` (release). `main` is pinned to the v0.11.0 release commit `dfb4983`, tagged `v0.11.0` (pushed 2026-09-25); further work resumes on `dev` and reaches `main` only at the next release.
- Runtime is Node.js ESM, Node `>=22`, no production dependencies, no build step. There are 28 outbound adapters and six inbound control channels.
- Inbound identity is composite and source-bound: `(channel,userId)` plus exact `accountId`/`chatId` when present. Unknown or missing source authority fails closed; channel names are never used as account IDs.
- The loopback Web/admin console is the only control console (`127.0.0.1` + Bearer). It supports personal-mode setup, pairing, channel test sends, masked question listing, and Control-Core choose/reject settlement. YAML remains an advanced/automation entry.
- Desktop `ask_user` has no safe host interface. Desktop fallback is the fail-closed outcome for timeout/error; do not claim desktop settlement or dual-end parity.
- Control Core, session arbiter, bounded approval members, session control overlay persistence, provider facades, callback capacity, and public notifier facade limits are code/contract-tested.
- Public discovery metadata is aligned with v0.10.2; the Awesome DSH listing refresh is tracked by PR #4981, and dshfind consumes the repository metadata on its scheduled sync. The v0.11.0 publication landed 2026-09-25, so the next listing refresh (Awesome DSH + dshfind scheduled sync) is now due and remains an external step.
- QQ C2C buttons, QQ GROUP text fallback, and QQ/WeChat iLink/DingTalk image envelopes are contract-tested only. Media/file support and provider-specific payload/reconnect behavior remain `declared` or unverified.
- `allowUsers` is retained as one-shot `inbound:migrated` compatibility import; optional Feishu/QQ SDK lifecycle seams are documented in [compatibility-matrix.md](../compatibility-matrix.md).
- DSH host compatibility is declared there too: `@deepseek-ai/dsh-session` is an **optional** peer marker (`peerDependenciesMeta.optional`) whose exact/OR-exact range `0.1.7-alpha.1 || 0.1.7-alpha.2 || 0.1.7-rc.1 || 0.1.7-rc.2` was generated from verified `dsh-v0.1.7-*` seam sources (no `^`/`>=` guessing; `0.1.0-rc.6` re-checked as unverified and excluded). `scripts/verify-host-compat.mjs`, wired into `npm run verify:release`, keeps the peer range, the matrix rows, and `dshWorkshop.compatibility.dshVersions` mechanically aligned; no real-host preflight run has been done yet (see [risks.md](risks.md)).

## Validation evidence

- `npm test`: 1816 total (1816 pass), 2026-09-25 (v0.11.0 release baseline).
- `node scripts/verify-release.mjs`: release guard compares package `0.11.0` with its documented 1816-test contract (badge / test text / homepage metadata / HANDOFF) and the host-compatibility matrix.
- `npm publish`: `dsh-notifier@0.11.0` published 2026-09-25 from `main` @ `dfb4983` (annotated tag `v0.11.0`, pushed), `latest` dist-tag moved `0.10.2 → 0.11.0`; shasum `851176a79df80878684ac9da6bd6ad62faeacec2` matches the locally built tarball byte-for-byte (243 files, 1.1 MB packed / 3.6 MB unpacked). Registry-artifact disposable-profile smoke: installed the published tarball in a clean profile (not a `file:` install) → `version 0.11.0`, `dshQuality.testCount 1816`, `dsh-notifier/testing` → `createFakeNotifier`, `types/index.d.ts` + `PLUGINS.en.md` + `docs/guide.md` + `cordis.patch.yml` present.

## Next release gate

v0.11.0 has shipped (git tag `v0.11.0` + npm `latest`). The next release line resumes on `dev`, and its gate reuses the same checklist: run `npm pack --dry-run --json` and a registry-artifact disposable-profile smoke (registry install in a disposable profile, not a `file:` install). Validate startup, one outbound test, one inbound command, and the `ask_user` assembly boundary. Real-device, provider, and DSH-host validation remains an external gate; see [risks.md](risks.md).