# Project state

> Workstream overlay (2026-09-26): source tree is on the `v0.13.0` development line. C11/C12 hardening and the control-surface convergence work are complete on `dev`; full `npm test` is **1984/1984 pass, 0 fail, 0 skip**, and `npm run verify:release` is green. No tag or npm publish was performed. This closeout adds no fresh real-device/provider-account evidence; those boundaries remain open in `risks.md`.

Historical snapshot: 2026-09-25. Release line **v0.12.0** (Native Control Surface + live outbound hot-apply + outbound state migration; **1831 tests, all pass**; **published**). It remains the prior release reference; the current v0.13.0 line preserves its canonical `channel:<type>:outbound` state and Native/Admin control-surface contract while adding the hardening recorded above. Previous release line **v0.11.0** (1816 tests, all pass). Test-count baselines in release guards are updated at train close, not per-commit.

- Current release validation: `npm test` = **1984** total, **1984 pass**, **0 fail**, **0 skip** (2026-09-26, v0.13.0 dev closeout); the previous v0.12.0 line was **1831** and v0.11.0 was **1816**.
- Historical npm `0.8.6` artifact carried contract count **909**; `0.9.0` used **1352**; v0.9.5 closed at **1531**; v0.9.6 closed at **1544**; v0.9.7 closed at **1548**; v0.10.0 closed at **1605**; v0.10.1/v0.10.2 were **1616**; the v0.11.0 line was **1816**; the v0.12.0 release line is **1831**.
- Single-repo two-branch model: `THEWOLFWALKER/dsh-notifier` hosts `dev` (development) and `main` (release). This v0.13.0 work is pushed to `dev` only; no tag, npm publish, or `main` update is part of this closeout.
- Runtime is Node.js ESM, Node `>=22`, no production dependencies, no build step. There are 28 outbound adapters and six inbound control channels.
- Inbound identity is composite and source-bound: `(channel,userId)` plus exact `accountId`/`chatId` when present. Unknown or missing source authority fails closed; channel names are never used as account IDs.
- The DSH Native `Notify & Control` surface is the primary daily control plane in v0.13 (Sidebar/Main + Plugins entry). The loopback Web/admin console remains `127.0.0.1` + Bearer and is Advanced / Recovery for pairing, bindings, sessions and deeper diagnostics. YAML/CLI remain automation/headless entries. Outbound saves Hot Apply through the single live `OutboundSource`, with epoch/revision and structured error semantics at the control surface.
- Desktop `ask_user` has no safe host interface. Desktop fallback is the fail-closed outcome for timeout/error; do not claim desktop settlement or dual-end parity.
- Control Core, session arbiter, bounded approval members, session control overlay persistence, provider facades, callback capacity, and public notifier facade limits are code/contract-tested.
- Public discovery metadata is aligned with v0.10.2; the Awesome DSH listing refresh is tracked by PR #4981, and dshfind consumes the repository metadata on its scheduled sync. The v0.12.0 publication landed 2026-09-25, so the next listing refresh (Awesome DSH + dshfind scheduled sync) is now due and remains an external step.
- QQ C2C buttons, QQ GROUP text fallback, and QQ/WeChat iLink/DingTalk image envelopes are contract-tested only. Media/file support and provider-specific payload/reconnect behavior remain `declared` or unverified.
- `allowUsers` is retained as one-shot `inbound:migrated` compatibility import; optional Feishu/QQ SDK lifecycle seams are documented in [compatibility-matrix.md](../compatibility-matrix.md).
- DSH host compatibility is declared there too: `@deepseek-ai/dsh-session` is an **optional** peer marker (`peerDependenciesMeta.optional`) whose exact/OR-exact range `0.1.7-alpha.1 || 0.1.7-alpha.2 || 0.1.7-rc.1 || 0.1.7-rc.2` was generated from verified `dsh-v0.1.7-*` seam sources (no `^`/`>=` guessing; `0.1.0-rc.6` re-checked as unverified and excluded). `scripts/verify-host-compat.mjs`, wired into `npm run verify:release`, keeps the peer range, the matrix rows, and `dshWorkshop.compatibility.dshVersions` mechanically aligned; the v0.12.0 release closed the real-host floor by booting `0.1.7-rc.2` and `0.1.7-alpha.1` (see [risks.md](risks.md) for the remaining provider-level gaps).

## Validation evidence

- `npm test`: 1984 total (1984 pass, 0 fail, 0 skip), 2026-09-26 (v0.13.0 dev closeout).
- Real DSH host `0.1.7-rc.2` (web profile): plugin activates; the Native Control Surface RPC channel answers `POST /dsh-notifier/surface.home` with `200` + the frozen `server-response` envelope; `channels.list` returns 28; the sidebar entry and Main panel render with zero `data-slot-error`; the channel setup wizard lists every type; `channels.save` + `channels.test` on a webhook drove a real SSRF rejection that the UI surfaced verbatim; and the save hot-applied with no restart (`revision` 1→4, `configured:true`/`active:true`/`applyMode:"hot"`, canonical `channel:webhook:outbound` persisted alone). An invalid save failed closed without bumping the revision. `alpha.1` floor: plugin activates, RPC answers `200`, 28 channels, `dsh-notifier/client.js` appears in the host client-module manifest with `immediately:true` and is served at `200` with the label-thunk fix.
- DSH host inject floor: the real host exposes `ctx.connection` and `ctx.webServer`, but `ctx.connection.rpc.handle` is unusable in the web profile (owner ctx lacks `webServer`), so the channel is mounted directly on the web server — the same seam the official gateway uses for `/api`.
- `node scripts/verify-release.mjs`: release guard compares package `0.13.0` with its documented 1984-test contract (badge / test text / homepage metadata / HANDOFF) and the host-compatibility matrix — green for the v0.13.0 dev closeout.
- `npm publish` (v0.12.0): `dsh-notifier@0.12.0` published 2026-09-25 from `main` @ `6d31d5c` (annotated tag `v0.12.0`, pushed to `origin`; `dev` also carries the release commit), `latest` dist-tag moved `0.11.0 → 0.12.0` (verified against `https://registry.npmjs.org/dsh-notifier` packument, not just the npm cache); shasum `e1499cc4a246921be4262aa49e5a8080377bf999` and `sha512-o43yYGn1+iCuD0BvAUHVt8+vzithsh7cUT6CwN8EXouMsaY00QXFJf1VVgaQAjbd38tZWH5EPUwBtQJ06mXQEQ==` match the locally built tarball byte-for-byte (262 files, 1.1 MB packed / 3.7 MB unpacked). Registry-artifact disposable-profile smoke: installed the published tarball in a clean profile (not a `file:` install) → `version 0.12.0`, `dshQuality.testCount 1831`, `exports["./client"] === "./client.js"`, `dsh-notifier/testing` → `createFakeNotifier`, and `types/index.d.ts` + `PLUGINS.en.md` + `docs/guide.md` + `cordis.patch.yml` + `client.js` present.
- `npm publish` (v0.11.0): `dsh-notifier@0.11.0` published 2026-09-25 from `main` @ `dfb4983` (annotated tag `v0.11.0`, pushed), `latest` dist-tag moved `0.10.2 → 0.11.0`; shasum `851176a79df80878684ac9da6bd6ad62faeacec2` matches the locally built tarball byte-for-byte (243 files, 1.1 MB packed / 3.6 MB unpacked). Registry-artifact disposable-profile smoke: installed the published tarball in a clean profile (not a `file:` install) → `version 0.11.0`, `dshQuality.testCount 1816`, `dsh-notifier/testing` → `createFakeNotifier`, `types/index.d.ts` + `PLUGINS.en.md` + `docs/guide.md` + `cordis.patch.yml` present.

## Next release gate

v0.12.0 has shipped (git tag `v0.12.0` + npm `latest`). The next release line resumes on `dev`, and its gate reuses the same checklist: run `npm pack --dry-run --json` and a registry-artifact disposable-profile smoke (registry install in a disposable profile, not a `file:` install). Validate startup, one outbound test, one inbound command, and the `ask_user` assembly boundary. Provider/real-device validation remains an external gate; see [risks.md](risks.md).
