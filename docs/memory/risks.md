# Recurring risks

- Mock/contract tests do not establish provider payload limits, callback shapes, callback byte limits, reconnect behavior, or optional SDK lifecycle behavior. Changes in those areas need protocol-level or real-device evidence.
- 2026-09-12 mobile-task-loop-v010 line (v0.10.0): native-question bridge (`createNativeQuestionBridge`) is wired to the host public seam `registerProvider`; the seam shape is from `@deepseek-ai/dsh-user-questions@0.0.1-rc.3` contract, but there is no real DSH-host run on this codebase — a host holding a different provider or a different `ctx.userQuestions` surface degrades to `unsupported`/fallback rather than bridging. QQ image parsing (`parseQQImageMessage`) is fixture-only and deliberately not wired to any adapter until the real QQ `extra` segment shape is confirmed (protocol discipline, not a defect). Image SSRF hardening (IPv4-mapped IPv6) and admin `/api/host` error-path degradation are contract-tested. Standing gap: run one real host smoke to confirm the provider register→ask round-trip and first-answer settlement, and one real QQ image event to flip on image parsing.
- 2026-09-12 Issue #23 fix line (v0.9.7): QQ gateway heartbeat resequenced so HELLO only records `heartbeat_interval` and IDENTIFYs, and the first beat only fires after READY/RESUMED (idempotent arm), with single-miss recovery still sending the next beat. The protocol basis is real-device A/B evidence recorded in the issue, but this implementation is mock/contract-tested only — no fresh real-device soak of the new HELLO→READY→beat→miss→recover→threshold-reconnect sequence has been run on this codebase. Standing gap: observe real traffic once for (1) gateway ACKs heartbeats sent pre-auth vs post-auth, (2) a single dropped ACK recovers without reconnect, (3) a late old-connection ACK does not disturb a new connection, (4) stop/restart re-arms cleanly. Residual: before this fix the pre-auth heartbeat doubled as a liveness watchdog — if the server sends HELLO then neither READY/close/op9 (silently hangs the connection), no heartbeat now fires and no separate handshake timeout exists, so reconnect depends on the TCP layer eventually erroring; accepted residual (the required post-auth-only startup removes the pre-auth watchdog), watch for `QQ 网关已就绪` absence on real traffic.
- 2026-08-28 R1 fix line (v0.9.1): DingTalk Stream protocol fixes (G-01 ack shape, G-02 SYSTEM/ping echo, G-10 `ua` field) are arbitrated from `dingtalk-stream` 2.1.4/2.1.6-beta.1/2.1.7-beta.1 source only, `docs/protocol-preflight/dingtalk.md` holds the evidence. SYSTEM ping marker uses `topic ∪ method ∪ event_type` union because the SDK uses `topic` while the review named `method`/`event_type`; no real frame sample can adjudicate. SDK ack frames also carry a top-level `message: "OK"` our implementation omits — real-device 10-minute Stream soak (ping period, no 60s re-push, ack acceptance) is the standing gap.
- 2026-08-28 R1 fix line: QQ `stripMention` whitelist (`<@!数字>`/`<@数字>`/行首 `@名字+空格`) has zero real-device samples; unknown mention shapes now log via `debug` and are kept verbatim — observe debug sampling for 72h on real traffic, extend the whitelist from captured shapes. Passive-reply quota 4/5 per msg_id is from official docs; overflow is a platform-silent drop, warn only marks "possibly dropped".
- 2026-08-28 R1 fix line: Feishu mentions-map restoration (G-25) relies on the event `mentions[].key/name` shape from official docs, not real payloads; events without mentions fall back to the old placeholder-delete behavior (mid-text double spaces may remain).
- 2026-09-24 #20 fix line (v0.11.0): Feishu P2P `ou_/oc_` identity equivalence (`isOpenIdTarget`/`feishuP2pEquivalent`, operator.open_id === srcChat, 裁决入账用 srcChat、补发仍用 oc_) is contract-tested only; the operator identity field union (`operator.open_id` ?? `sender.sender_id.open_id`) and `context.open_chat_id`/`open_chat_type` shapes come from official docs/PR #20 真机事故证据, not fresh real payloads on this codebase. Standing gap: one real Feishu P2P card click + private-chat numbered reply to confirm the operator field path and `chat_type='p2p'` actually arrive as modeled.
- 2026-08-28 R2 fix line (v0.9.2): QQ gateway close-code branch table (G-07: 4004 invalidate+re-IDENTIFY, 4008 fixed 60s, 4006/4007/4009 drop session) and op9 `d` flag resume semantics (G-21) are from official docs/SDK lineage only — no real close frame has ever been observed on this codebase. A real soak that survives a 4008 window and an INVALID_SESSION resumable path is the standing gap.
- 2026-08-28 R2 fix line: iLink polling watchdog (G-12) fires on "in-flight getupdates beyond longPollTimeoutMs + grace"; real iLink servers may legitimately hold long polls near the 35s mark — grace is ≥5s and watchdogIntervalMs defaults to 15s, but only real traffic can confirm no false-positive aborts (watch for `轮询假死检测` warns during normal operation).
- 2026-08-28 R2 fix line: G-55 makes illegal upstream TTL (`expires_in` ≤ 0 / non-numeric) fail-closed. All known providers return 7200s so this should never fire; if a provider ever returns fractional-second or string TTLs, the strict contract will surface it as a delivery error instead of a silent default — triage before relaxing.
- 2026-08-28 R3 fix line (v0.9.3): SSRF guard (S-02) has a theoretical DNS-rebinding race between `urlguard` lookup and `fetch` connect — closing it fully needs a custom dispatcher pinning the resolved IP, which violates the zero-dependency budget. The 60s short-TTL lookup cache shrinks the observation window; accepted residual at medium severity.
- 2026-08-28 R3 fix line: `allowPrivateNetwork: true` and the onebot `private-ok` escape hatches do not defend against an attacker who already holds the admin token (they can flip the flag and the URL together) — single-token model boundary (S-09 scope), not an urlguard gap.
- 2026-08-28 R3 fix line: S-05 secret masking (`redact.mjs`) is shape-based; novel token formats outside JWT/sk-/ghp_/xox/AKIA/Bearer/long-hex/long-base64 shapes pass through. `redaction: 'extended'` disables masking entirely by explicit user choice (data-flow direction documented in README).
- 2026-09-24 #31 fix line (v0.11.0): QQ/钉钉入站 transport 已注入有限超时（AbortController+signal，`config.timeoutMs` 默认 10000，超时 AbortError 由调用方 catch、不盲目重试）。飞书入站（Host P0 收口）：`@larksuiteoapi/node-sdk@1.73.0` 公开 `Client({httpInstance})`，`defaultHttpInstance` 可从 SDK 导入——dsh-notifier 注入一个仅属于本 Client 的隔离 bounded wrapper，在底层 `HttpRequestOptions` 层强制 <=10s transport timeout（覆盖 message create/patch 及其 tenant token 获取链路）；不修改 SDK 共享 `defaultHttpInstance`、不用 `Promise.race`、不盲重试。证据：npm `@larksuiteoapi/node-sdk@1.73.0` README（Client httpInstance 参数）；official SDK `typings/http.ts`（`HttpRequestOptions.timeout?: number`）。Standing gap: 真机确认 1.73.0 `defaultHttpInstance` 方法表（request/get/delete/head/options/post/put/patch）与其文档一致（wrapper 显式绑定全方法；`request()` 为 hard gate）。
- The v0.9.7 (pr22-issue23-fix) release line has **1548** tests (1548 pass) and is published, but no real provider/device or DSH-host protocol run. Do not label any inbound provider `real-device-verified`.
- Real-device handoff remains: Telegram 4096 UTF-16 boundary; Feishu WS and QQ gateway/DingTalk stream reconnects; QQ callback ACK and keyboard limits; WeChat iLink QR/long-poll/context-token behavior; WxPusher callback shape; image/file payloads; and provider delivery limits.
- Windows desktop toast cannot run in headless CI; validate BurntToast/PowerShell locally when touching `desktop`. Desktop `ask_user` has no safe host interface, so desktop settlement and dual-end parity must not be advertised.
- The loopback admin server is bound to `127.0.0.1` and Bearer-gated. Its question list is masked and its choose/reject endpoint must always go through Control Core; no direct ledger or answer write is allowed.
- `notifyAll().delivered` is channel-level evidence only. Numbered question fallback needs target-level delivery evidence; wrong-chat and missing-chat replies are consumed fail-closed.
- WxPusher injects a local `accountId` from configuration, defaulting to literal `default`; multiple apps must configure distinct local IDs or they are indistinguishable within that channel namespace. Callback `data.appId` is never trusted as identity.
- State files contain bindings, pending work, and credentials. Keep them private, use store locking/merge setters, and do not edit a live state file manually. Bootstrap pairing codes are short-lived `0600` files; same-user/root access remains the host trust boundary.
- `wechat:ctx:` and inbound debounce/dedup tables are bounded caches. Eviction order and user-visible early flush behavior have only mock evidence and should be observed on real traffic before changing limits.
- Same-process DSH plugins are trusted code, not an OS isolation boundary. Facade freezing, budgets, audit metadata, and package export narrowing reduce supported-path misuse but cannot protect against a hostile plugin reading host memory or importing internal constructors.
- The package artifact and engineering archive can drift. Resolve any `CHANGELOG.md` or file-list mismatch before publishing; registry acceptance must use a registry install in a disposable profile, not a `file:` install.
- 2026-09-17 #27 fix line: the native-questions bridge attaches on real hosts via the `user-questions/request` waterfall interceptor (`{prepend:true, global:true}`; `registerProvider` kept for hosts that expose it). `dsh-user-questions@0.1.5-rc.2` exposes `ask()` only — the old registerProvider-only bridge degraded `no_register_provider` on every real host. **Known host-side limitation**: when the interceptor answers first, the host `ask()` never aborts its signal, so the GUI remote card is NOT retracted (dsh-api-gateway retracts only on client-result / signal-abort / ctx-release). Fix belongs in DSH core (`ask()`: AbortController linked from `request.signal`, abort on settlement — the downstream cancel chain already exists); plugin degrades honestly, phone/admin surfaces unaffected. Downstream-win close is an interception-scoped AbortSignal into `askQuestions` (rounds 3-5): push timers cleared, escalation stopped, the row explicitly terminated before `bus.abandon`, later questions in a multi-question request never open, hinted/broadcast fallbacks gated, and late-delivered cards reconciled with terminal-state wording. Cancellation guarantee (2026-09-17, review round 5, honest wording): no NEW question delivery starts after cancellation (cards, broadcast fallback, hinted numbered text); in-flight HTTP sends cannot be unsent — late successes are finalized best-effort by the runPush exit path (terminal-state card edit). The interceptor is contract-tested on a mock waterfall and verified on a real host: the contributor's daily profile ran `codex/native-questions-waterfall` end to end (boot line 宿主原生提问桥 已 attach（waterfall）; harness-native `ask_user_question` settled once from either surface — Telegram card or web GUI; single-select Telegram button card and multi-select numbered fallback at parity). The standing real-host gap from the 2026-09-12 line is closed.
- 2026-08-28 W11 fix line: G-19 scoped-event contract is **evidence-logged, not code-changed**. C-round review recorded cordis source-level evidence (`vendor/cordis/src/events.ts:165-175` filters listeners by context scope; `core/agent/src/runtime-types.ts:175` declares `agent/disposed` Scoped), and the 02 问题清单 notes root subscription may miss scoped events or hear out-of-scope ones — in tension with `selectHostEventContext` switching to root. That vendor source is not in this repo and no real DSH-host run has confirmed which scope `session/event`/`agent/disposed` are emitted under. Per plan, behavior stays: scope-tagged diagnostics (`scopeDiagnosticOf`) + root fallback + tolerant payload parsing, with a standing real-device regression item (`scoped agent error 插件可收`) before any strategy change (root / dual-subscribe / payload fallback).

Authoritative details: [project-state.md](project-state.md), [docs/protocol-preflight/](../protocol-preflight/), [docs/security/](../security/), and [docs/compatibility-matrix.md](../compatibility-matrix.md).
