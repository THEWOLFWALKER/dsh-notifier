# Workstream: v0.12-native-control-surface

- Agent identity: `trae | TraeCode | remote sandbox (linux)`
- Agent: `trae`
- Branch: `dev`（v0.12 任务书 Commit discipline #6 明确要求「每个提交推送到 dev」；本仓库 AGENTS.md 默认的 `codex/<topic>` 分支模型在这个交付里被任务书显式覆盖，`main` 保持发布线不动）
- Status: `active`
- Start/end: `2026-09-25 -> active`
- Scope: 按 v0.12 交付包任务书实现 Native Control Surface（20 个提交，`dev` 上逐个落地并推送）。
- Plan: 提交顺序 = 交付包 `docs/TASKBOOK-v0.12.0.md` 的 Commit 1..20；每个提交先写聚焦测试、跑该提交的 focused gate，再推送并更新本记录。任务书 §7 要求的对抗评审检查点在 Commit 6 / 9 / 14 / 18 / 20 之后执行。
- 交付包基线: `dsh-notifier-v0.12-delivery.zip` sha256 `fca36585f2e6c8e70227c850e5d70881895df71187522bac78cdfcec138fba90`（已核对）；任务书声明的基线是 `dfb4983`，落地时 `dev` 已前进到 `7139a6b`（仅 v0.11 发布事实的文档提交，与任务书基线无源码差异）。
- Owned files: `plugin-src/client/**`, `lib/client.js`, `scripts/verify-client.mjs`, `scripts/verify-release.mjs`（仅在需要时扩展）, `src/control-surface/**`, `src/runtime/outbound-source.mjs`, `src/index.mjs`（接线）, `package.json`, `.github/workflows/ci.yml`, `test/*client*`, `test/*control-surface*`, `docs/reliability.md`, 本记录。
- Do not touch: `src/admin/**` 的大规模重写（任务书硬规则：`src/admin/api.mjs` 在 v0.12 保持兼容消费者形态）、`main` 分支、`package-lock.json` / `node_modules`、渠道适配器行为（v0.12 不加渠道）。
- Validation: `npm test`；`npm run build:client`；`npm run verify:client`；`node scripts/verify-release.mjs`；`node scripts/gen-channel-matrix.mjs --check`；`node --check`；`git diff --check`；`npm pack --dry-run`。
- Adversarial review: 见下方每个提交的 review 记录；任务书要求的 5 个检查点单独记录。
- Handoff: 进行中。`main` 停在 `dfb4983`（= tag `v0.11.0`），v0.12 全部工作留在 `dev`，未发布。

## 提交记录

### Commit 1 — `build(client): add DSH browser module pipeline`

- 交付内容：`package.json`（`exports["./client"]`、`files` 增 `lib/client.js`、`build:client` / `verify:client` 脚本、`devDependencies.esbuild=0.25.9`、`dsh.client{platform,inject}`）、`plugin-src/client/build.mjs`（交付 starter 原文）、`plugin-src/client/index.js`（最小入口）、`scripts/verify-client.mjs`（任务书 10 条断言）、`test/client-build.test.mjs`（10 个静态契约测试）、`lib/client.js`（1974 字节产物）、CI 新增 `client` job + `dev` 触发。
- 决策（任务书内部矛盾的解法）：Commit 1 的构建目标 `lib/client.js` 需要入口 `plugin-src/client/index.js`，但该文件按任务书属于 Commit 2 的「Copy from delivery starter」清单。为保持两个提交都窄且都绿，Commit 1 只放一个**最小入口**（声明 `name` / `inject`，引用 Host external `react`，不注册插槽、无 UI、无 mock 数据），Commit 2 用交付的完整外壳替换它并重生产物。
- 决策（CI 范围）：任务书要求「Build again in CI and compare bytes to catch drift」，但原 CI 只在 `main`/`master` push 与 PR 触发，`dev` 上的工作完全没有 CI 覆盖。故新增 `client` job（唯一需要 `npm install --no-package-lock --ignore-scripts` 的位置）并让 `dev` 也触发 CI。`test` job 保持零安装直跑 `node --test`，`test/client-build.test.mjs` 刻意不 import esbuild。
- Tests: `node --test test/client-build.test.mjs` → 10 pass / 0 fail；`npm test` 全量结果见提交时的门禁记录。
- Review findings: 断言 #4 的「不要第二份 React」用 React 内部标记（`ReactCurrentOwner` 等）+ `require("react")` 双向校验；#6 的 `eval`/`new Function` 与 #7 的绝对路径在 1974 字节占位产物上无法真正证伪（内容太少），**这两个断言要在 Commit 2 的真实外壳产物上重新确认才有意义**——已登记为待复验项。
- 遗留风险: 占位入口会在 Commit 2 被替换；`dsh.client` 是 Host 侧清单，尚无真实 DSH 宿主跑过（真机门在 Commit 20）。