# Workstream: native-questions-waterfall

- Agent identity: `captain-dsh | glm-5.3 (zai) | mcaipgx02 (chinto's Mac)`
- Agent: `captain-dsh`
- Branch: `codex/native-questions-waterfall`
- Status: `done`
- Start/end: `2026-09-17 -> 2026-09-17`
- Scope: make the native questions bridge attach on real DSH hosts (waterfall seam), fix #27's probe crash, and give askQuestions interception-scoped cancellation with terminal-faithful late cleanup.
- Plan: (1) defensive service reads + ctx.inject optional-dep attach + waterfall interceptor; (2) AbortSignal into askQuestions with delivery guards and late-delivery reconciliation; (3) contract tests + deterministic regressions; (4) docs sync.
- Owned files: `src/host/native-questions.mjs`, `src/host/capability.mjs`, `src/index.mjs` (native bridge block), `src/questions/router.mjs`, `test/native-questions.test.mjs`, `test/host-capability.test.mjs`, `test/questions-web-first.test.mjs`, `CHANGELOG.md`, `docs/memory/risks.md`, `HANDOFF.md`, this file.
- Do not touch: approval paths, channel adapters, `src/control/*`.
- Validation: `npm test` (1639 pass), `node scripts/verify-release.mjs`, `node scripts/gen-channel-matrix.mjs --check`, `node --check src/index.mjs`.
- Adversarial review: CodeRabbit rounds on PR #30 (base-branch convention, race/lifecycle findings, late-delivery semantics — all addressed and confirmed); independent external design critique before implementation (cordis waterfall semantics, gateway retraction triggers, inject footgun).
- Handoff: `done`. Squashed history: 0a4cfac (fix(host): bridge attach — defensive reads, ctx.inject optional dep, waterfall interceptor) + 452b42a (fix(questions): interception-scoped cancellation, delivery guards, finalTextOf terminal presentation) + docs commit (CHANGELOG/risks/HANDOFF/this record). Real-host canary passed on the contributor's daily profile (boot line 宿主原生提问桥 已 attach（waterfall）, dual-surface ask_user_question end to end, single-select button card + multi-select numbered fallback parity). PR #30 (base main) awaiting maintainer review; known host-side limitation: GUI card not retracted when the bridge answers first (DSH-core ask() signal semantics — sketch in risks.md and the #27 discussion).
- Follow-up (2026-09-17, post-squash; pushed as 944b1f2 and included in PR #30): CodeRabbit out-of-diff Major — the `webFirstMs=0` direct path `await`ed `runPush()` before entering `waitPromise`, so one held per-target card send kept `askQuestions` from returning after any settlement (answer/timeout/interception-abort). Fix: direct path detached exactly like the timer path (`runPush().catch(warn)`); `cancelDelivery()` still gates new sends post-settlement and late successes still reconcile through the runPush exit path. Semantic unification: a direct-path push error now warns and leaves the question to Web/timeout instead of returning `error` immediately (same fail-closed as the delayed path). Test +1 in `test/questions-web-first.test.mjs` (direct path, two gated targets, hold B, settle via adminSettle, `askQuestions` returns immediately, B reconciles with answered wording; proven red on unfixed code via 2s race guard). Full suite 1637.
- Follow-up 2 (2026-09-17, review of 944b1f2 landed 14:37Z; commit pending push approval): Major — hinted numbered-text fallback pushed ALL `sendText` calls synchronously before `Promise.all`, so cancellation during the first in-flight send could not stop targets 2..N; now sequential per-target await with an `isCancelled()` recheck before each next send (same gate shape as card delivery), `hintedTargets` still records only confirmed sends. Minor — `attachError` cleared on both successful (re)attach paths (provider + waterfall) so `capabilities()` never reports a stale failure after a healed replay. Docs — CHANGELOG waterfall bullet now points to the final interception-scoped AbortSignal close-out (the early `adminPending`/`adminSettle` heuristic is superseded) and the v0.10.1 "only seam" claim gained a waterfall note; this record's stale "pending push approval" status fixed. Tests +2 (`test/questions-web-first.test.mjs` hinted held-send abort — second target never starts, proven red on unfixed code; `test/native-questions.test.mjs` fail-then-heal re-attach clears the error on both seams). Full suite 1639. Out-of-scope review finding (finite timeouts on Feishu/QQ-inbound/DingTalk inbound transports) deferred to a follow-up issue — channel adapters are on this workstream's do-not-touch list.
