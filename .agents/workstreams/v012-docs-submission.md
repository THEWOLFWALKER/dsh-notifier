# Workstream: v012-docs-submission

- Agent identity: `codex | DeepSeek-V4.1-Flash | TraeCode remote sandbox`
- Agent: `codex`
- Branch: `main` (docs-only refresh against the locked release baseline; no `codex/*` branch because the pack's fail-closed script asserts `HEAD == d4b7d54`)
- Status: `done`
- Start/end: `2026-09-25 -> 2026-09-25`
- Scope: Integrate the `dsh-notifier-v0.12-ALL-SUBMISSION` pack — Phase 1 repository docs/brand/discovery refresh, then Phase 2 ecosystem discovery metadata (`GITHUB-METADATA-ACTION.md`) and Phase 3 the two prepared directory PRs.
- Plan: verify the pack (`verify-all.mjs` + `MANIFEST.sha256.json`) → run `01-REPO-SUBMIT/scripts/apply-repo-refresh.mjs` against `main` @ `d4b7d54` → fix any pack defects that break the repository gates → re-run every gate → commit and push → apply GitHub repository description/topics → open the prepared PRs.
- Owned files: `README.md`, `README.zh-CN.md`, `docs/guide.md`, `docs/OPERATIONS.md`, `docs/compatibility-matrix.md`, `docs/upgrade-guide.md`, `docs/upgrade-guide.en.md`, `docs/assets/*`, `docs/KNOWLEDGE_BASE.md`, `docs/TECHNICAL_DEBT.md`, `docs/ROADMAP.md`, `docs/memory/project-state.md`, `HANDOFF.md`, `package.json` (description/keywords only).
- Do not touch: `CHANGELOG.md` history, `src/**`, `test/**`, published npm artifacts, historical release tags.
- Validation: `node scripts/gen-channel-matrix.mjs --check`; `node scripts/verify-host-compat.mjs`; `node scripts/verify-release.mjs`; `git diff --check`; `npm test` (1831/1831, exit 0); `npm pack --dry-run --json` (262 files, `client.js` present, 0 agent-config leaks).
- Adversarial review: the delivered pack failed its own gate list; five defects were fixed rather than papered over.
  1. `01-REPO-SUBMIT/scripts/apply-repo-refresh.mjs` did not parse at all — `new = '…'` used the reserved word `new` as an identifier, so the whole module threw `SyntaxError: Unexpected token '='` and **nothing** applied. Renamed to `replacement` (both the assignment and the `replaceOnce` call site). Confirmed with `node --check` + `verify-pack.mjs` before re-running.
  2. The new `README.md` / `README.zh-CN.md` silently dropped the machine-checked markers that `scripts/verify-release.mjs` depends on, turning the release guard red (6 test-count markers + 2 metadata-version markers → `null`). Restored the guard's contract in the new voice: a `Package metadata: \`dsh-notifier@0.12.0\` · 1831 automated contract tests · MIT licensed.` line (and its `包元数据：…` zh twin) plus a `test/ 1831 tests …` line (zh `test/ 1831 个测试 …`) — the same anti-drift markers added by the 2026-09-05 review, not a weakened guard.
  3. The new `docs/compatibility-matrix.md` deleted the fenced `json dsh-host-matrix` block outright, so `scripts/verify-host-compat.mjs` reported `has no parseable dsh-host-matrix block` — a host-compatibility contract regression, not a docs nit. Re-inserted the exact machine-readable block (peer package, four `verified` rows, one `unverified` row) under the human-readable table.
  4. Three overlay files carried markdown hard-break trailing whitespace (`docs/guide.md`, `docs/upgrade-guide.md`, `docs/upgrade-guide.en.md`), so the pack's own `git diff --check` gate failed. Converted the hard breaks into in-blockquote paragraph separators (`>` blank line) — same visual structure, no trailing whitespace.
  5. `apply-repo-refresh.mjs`'s TECHNICAL_DEBT logic intended to *replace* the release-state paragraph but anchored on the blank line after the title, leaving the stale v0.11.0 paragraph duplicated under the new v0.12.0 one. Removed the superseded paragraph (the pack's stated intent).
- Handoff: Phase 1 landed on `main` (docs/brand/discovery metadata only; version and runtime untouched, `npm test` unchanged at 1831/1831, all four repository gates green). Phases 2–3 require GitHub repository-settings write and third-party repo write that this environment does not hold: the addressable metadata card (`02-ECOSYSTEM-UPDATE/GITHUB-METADATA-ACTION.md`) and the two prepared PR payloads (`targets/awesome-dsh-plugin/`, `targets/ZeroPointRepo-awesome-dsh-plugins/`) are left unapplied for the owner; dshfind stays PR-free per its scheduled sync. Do **not** republish npm `0.12.0` or retag `v0.12.0`.